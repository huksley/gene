node_modules
.env.development

# Gene pipeline runtime state
.gene/
# Local Postgres state store (initdb'd by `npm run prepg`, data lives here)
data/
# Local clones of target repos (kept across runs, branched into worktrees)
repos/
